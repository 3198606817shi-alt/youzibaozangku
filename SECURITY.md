# 安全说明

## 凭证存储

- 小红书Cookie和硅基流动API密钥只保存到当前用户的Mac钥匙串。
- 飞书Base和数据表标识不是登录凭证，保存在Application Support目录的`settings.json`中。
- 发布包不包含任何使用者的Cookie、API密钥、飞书授权令牌或历史下载内容。

不要通过聊天、邮件、截图或公开文档发送Cookie、API密钥和飞书授权信息。

## 泄露后的处理

1. Cookie泄露：退出小红书全部登录设备，重新登录并更新Cookie。
2. 飞书授权异常：运行`lark-cli auth logout --json`清理本机登录，并在飞书授权管理中撤销应用授权。
3. 转写密钥泄露：在硅基流动后台立即重置密钥，并检查调用和余额记录。
4. 本地设备丢失：远程锁定Mac，修改系统登录密码，撤销相关账号会话。
5. 清理本地数据前先确认是否需要备份；删除后可能无法恢复。

## 本机服务

网页服务只监听`127.0.0.1:8766`，不会开放到局域网或公网。POST请求会检查来源，启动器也只会终止已经核实属于本工具的进程。

发现安全问题时，请私下联系仓库所有者，不要在公开Issue中粘贴凭证、Cookie或含个人数据的日志。
